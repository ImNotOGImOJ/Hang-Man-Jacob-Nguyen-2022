#include <vector>
#include <iostream>
using namespace std;
void display_misses(int misses);
void greet();
string end_game(string answer,string codeword);
void display_status(vector<char> incorrect, string answer);
string generate(string answer, string codeword);